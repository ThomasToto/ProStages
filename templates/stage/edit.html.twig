{% extends 'base.html.twig' %}


{% block contenu %}
{% block titrePage %}
    <div class="card-body">
        <h5 class="card-title">Modifier stage</h5>
{% endblock %}
<p class="card-text">
			 {{ include('stage/_form.html.twig', {'button_label': 'Modifier'}) }}
			</p>
			</div>

   

    <a href="{{ path('stage_index') }}" class="btn btn-primary" >Retour à la liste des stages</a>

    {{ include('stage/_delete_form.html.twig') }}
{% endblock %}
