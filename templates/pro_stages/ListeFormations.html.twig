{% extends 'base.html.twig' %}

{% block contenu %}

<div class="row" style="margin-top:15px:">
<table class="table">
            <thead>
             <tr>
                <th scope="col"></th>
                <th scope="col">Nom</th>
                <th scope="col">Liens</th>
                
             </tr>
            </thead>
      {% for formationCourante in formations %}
            <tbody>
             <tr>
                <th scope="row"></th>
                <td>{{formationCourante.nom}}</td>
                <td><a href="{{path('ProStages_Stages_Par_Nom_Formation', {'nomFormation':formationCourante.nom})}}" class="btn btn-primary">Voir les stages de la formation</a></td>
             </tr>
            </tbody>  
      {% endfor %}
</table>
</div>

{% endblock %}
